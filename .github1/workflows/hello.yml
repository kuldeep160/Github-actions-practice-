# workflow
# should have name
name: Hello

on:
  workflow_dispatch:
  
#jobs
# should be inside the workflow
jobs:

  say-hello:
  
    runs-on: ubuntu-latest
    
    steps:
      -name: say hello to everyone
       run: echo "Hello Dosto"
